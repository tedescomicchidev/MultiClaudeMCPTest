{
  "mcpServers": {
    "claude-code-docker": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-v", "/workspace:/workspace",
        "claude-mcp:latest",
        "claude", "mcp", "serve"
      ]
    }
  }
}
